

## AuthJS

This is a simple authentication system that allows users to log in and sign up. It uses a JSON file to store user information.

### Running the program

1. Clone the repository: `https://github.com/GicheruBenjamin/Authjs.git`
2. Navigate to the project directory: `cd Authjs`
3. Run the program: `npm run prod`